# 2026-05-03 テンプレート再調整 実施計画

## 概要
- `bi-girl.net` の画像表示を復活
- 全ソース共通でXへの誘導ボタンを強化
- 動作しないXタイムライン埋め込みコードの削除

## タスク

### 1. main.py の修正
- [ ] アイテムごとに `source_type` や `source` をチェックするロジックの追加
- [ ] `image_html` を `bi-girl.net` の場合にのみ生成するように変更
- [ ] `timeline_html` を削除し、代わりに `x_button_html` を追加
- [ ] コンテンツの組み立て順序を調整

### 2. テストの更新
- [ ] `tests/test_main.py` の既存テストを新テンプレートに合わせて修正
- [ ] `bi-girl.net` の場合に画像が含まれることを検証するテストケースの追加

### 3. 動作確認
- [ ] `python main.py --dry-run` でHTML構造を確認
- [ ] GitHub Actions 経由でテスト投稿

## 実装詳細
- ボタンの色: 黒 (#000) または Twitterブルー (#1DA1F2) を検討（現在は黒が一般的）
- ボタンのテキスト: 「🐦 X (Twitter) で最新の投稿を見る」
